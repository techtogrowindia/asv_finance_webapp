# Frontend build-time config. Copy to .env for local dev if needed.
# In production the app uses same-origin "/api/v1" (nginx proxies to the API),
# so this can be left as the default.
VITE_API_BASE="/api/v1"
